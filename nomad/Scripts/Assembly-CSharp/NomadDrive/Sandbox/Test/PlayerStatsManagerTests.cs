using UnityEngine;

namespace NomadDrive.Sandbox.Test
{
	public class PlayerStatsManagerTests : MonoBehaviour
	{
		private void Update()
		{
			Input.GetKeyDown(KeyCode.H);
			Input.GetKeyDown(KeyCode.T);
		}

		public void Deneme()
		{
		}
	}
}
